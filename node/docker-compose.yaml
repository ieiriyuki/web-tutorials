version: '3.9'
services:
  app:
    image: node:lts-slim
    container_name: mynode
    ports:
      - 8888:8888
    volumes:
      - ./app:/app
    working_dir: /app
    stdin_open: true
    tty: true
