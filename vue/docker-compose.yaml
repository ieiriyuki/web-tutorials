version: '3.9'
services:
  app:
    image: my-vue
    build:
      context: .
    environment:
      - NODE_ENV=development
    ports:
      - 80:80
      - 8000:8000
      - 8080:8080
      - 8888:8888
    volumes:
      - ./app:/app
    stdin_open: true
    tty: true
